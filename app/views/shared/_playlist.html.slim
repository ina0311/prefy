= link_to api_v1_playlist_path(item.id), class: 'card border-dark mb-3' do
  .card-body.text-dark
    - if item.image.present?
      = image_tag item.image, size: '100x100', class: 'card-thumbnail'
    - else
      = image_tag 'default_image.png', size: '100x100', class: 'card-thumnail'
    h5.card-title
      | "#{item.name}"
