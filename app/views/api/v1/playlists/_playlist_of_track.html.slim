div class="track d-flex position-#{playlist_of_track.position}"
  .col-1
    = image_tag playlist_of_track.track.decorate.image, class: 'track-album-image', size: '40x40'
  .col
    .track-name
      = playlist_of_track.track.name
  .col
    .track-artist-name
      = playlist_of_track.track.decorate.artists
  - if action_name == 'show'
    .col
      .track-album-name
        = playlist_of_track.track.album.name
    .col
      .track-playtime
        = playlist_of_track.track.decorate.convert_ms_to_min_and_sec
  - else
    .col
      .track_delete_button
        = link_to '削除',
          api_v1_playlist_playlist_of_track_path(id: playlist_of_track, playlist_id: playlist_of_track.playlist_id),
          data: { confirm: 'この曲を削除しますか？' },
          method: :delete,
          class: 'btn btn-danger',
          remote: true